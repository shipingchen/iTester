BUILD/CLEAN INSTRUCTIONS:
Use Microsoft Visual Studio for building and cleaning

RUN INSTRUCTIONS:
These file must be in the "<direc>/LoadTester2/LoadTester/bin/Debug" folder, where 
<direc> is the name of the directory unzipped to.

bin/Debug/WebFetch.dll
The HTTPResponse assembly is then included in the config.xml file to run.